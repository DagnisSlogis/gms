import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';


export const AboutUs = () => (
  <div>Par mums</div>
)

export const AboutUsConnected = connect()(AboutUs);
