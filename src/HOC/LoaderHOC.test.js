import React from "react";
import ReactDOM from "react-dom";
import LoaderHOC from "./LoaderHOC";

describe("LoaderHOC(prop)(Component)", () => {
  it("true", () => {
    expect(true).toBe(true);
  });
});
